# il file unico da mandare via WhatsApp si rigenera con build-standalone.py
dist/

.DS_Store

# le presentazioni restano fuori: il repo è pubblico (serve a GitHub Pages),
# e un pitch deck non va pubblicato per sbaglio con un git add -A
*.pdf
